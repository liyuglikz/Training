#include <iostream>
#include <string>
#include <algorithm>

#include <vector>

#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cmath>

#define MIN(a, b) (a < b ? a : b)
#define MAX(a, b) (a > b ? a : b)


using namespace std;

typedef pair<int, int> Point;
const int MAXN = 1e4 + 5;

vector<Point> lib[MAXN];

int main()
{
}
